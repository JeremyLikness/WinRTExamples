<Page
    x:Class="LiveConnectExamples.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LiveConnectExamples"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="UserName" Text="Not Signed In" Style="{StaticResource HeaderTextStyle}" Margin="15"/>
        <Image Grid.Row="1" Width="100" Height="100" Stretch="Fill" x:Name="ProfilePicture" HorizontalAlignment="Left" Margin="15"/>
        <StackPanel Grid.Row="3" Margin="15" Orientation="Vertical">
            <TextBlock Text="Age: " Style="{StaticResource PageSubheaderTextStyle}"/>
            <TextBlock Text="Unknown" x:Name="UserAge" Style="{StaticResource PageSubheaderTextStyle}"/>
        </StackPanel>
    </Grid>
</Page>
